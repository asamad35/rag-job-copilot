{
  "name": "rag-job-copilot",
  "version": "1.0.0",
  "description": "Chrome extension V1 form scanner and test autofill",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && cp manifest.json dist/manifest.json && cp dist/src/popup/popup.html dist/popup.html && rm -rf dist/src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "^0.1.37",
    "@types/node": "^25.3.1",
    "jsdom": "^28.1.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}
